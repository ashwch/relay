{
  "_comment": "Minimal external artifact verifier plugin. Demonstrates the verify hook: compares expected_asset_names from plugin config against release.artifacts in the shared release document. Returns error_message with missing asset names when verification fails. For authoring guidance, see docs/plugin-authoring.md.",
  "api_version": "relay.plugin/v1",
  "name": "example-verifier",
  "type": "artifact_publisher",
  "plugin_version": "1.0.0",
  "plugin_api_version": 1,
  "framework_version_range": "^0.2.0",
  "entrypoint": {
    "kind": "module",
    "handler": "index.mjs"
  },
  "capabilities": ["verify_assets"],
  "hooks": ["verify"],
  "supported_release_modes": ["framework-managed", "tool-observe"],
  "config_schema": "config.schema.json",
  "required_inputs": [],
  "required_secrets": [],
  "optional_secrets": [],
  "permissions": {},
  "supports": {
    "dry_run": true,
    "local": true
  },
  "outputs": ["example-verifier-result"],
  "trust": {
    "level": "external-allowlisted",
    "allow_in_ci": true
  }
}
